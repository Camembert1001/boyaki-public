# AI-STAGING developer diagnostics

These pages are diagnostic/test surfaces, not part of the Human UX or primary navigation.

- `e2e-selftest.html` runs the isolated backend E2E runner.
- `ui-dom-selftest.html` exercises the current BOYAKI conversation UI against browser-local fixtures.

Keep technical environment language here; do not copy it into ordinary user-facing pages.
